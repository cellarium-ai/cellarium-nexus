from setuptools import setup, find_packages

setup(
    name="cellarium-nexus",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Your requirements will be read from requirements/backend.txt
    ],
)
